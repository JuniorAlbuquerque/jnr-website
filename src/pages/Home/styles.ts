import { css } from '@/styles/stiches.config'

export const styles = {
  content: css({
    gap: '$3',

    h2: {
      fontSize: '$7',
      marginBottom: '$4'
    },

    p: {
      fontSize: '$3',
      textAlign: 'justify',
      lineHeight: '2.4rem'
    }
  })
}
